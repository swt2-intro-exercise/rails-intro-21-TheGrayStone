<%= link_to 'New', new_author_path %>
<table>
<tr>
<th>Name</th>
<th>Homepage</th>
<th>Actions</th>
</tr>
<% @authors.each do | author | %>
<tr>
    <td><%= link_to author.name, author_path(author) %></td>
    <td><%= author.homepage %></td>
    <td><%= link_to 'Edit', edit_author_path(author) %><%= link_to 'Delete', author_path(author), method: :delete %></td></tr>
<% end %>
<tr></tr>
</table>
